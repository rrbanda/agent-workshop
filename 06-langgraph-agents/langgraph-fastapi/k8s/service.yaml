apiVersion: v1
kind: Service
metadata:
  name: langgraph-fastapi
  labels:
    app: langgraph-fastapi
spec:
  type: ClusterIP
  selector:
    app: langgraph-fastapi
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
