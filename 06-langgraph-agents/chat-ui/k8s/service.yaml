apiVersion: v1
kind: Service
metadata:
  name: simple-agent-chat-ui
  labels:
    app: simple-agent-chat-ui
    app.kubernetes.io/name: simple-agent-chat-ui
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: simple-agent-chat-ui
