{{- if and .Values.langgraphFastapi.enabled .Values.langgraphFastapi.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.langgraphFastapi.route.name }}
  labels:
    app: {{ .Values.langgraphFastapi.name }}
spec:
  to:
    kind: Service
    name: {{ .Values.langgraphFastapi.service.name }}
  port:
    targetPort: http
  tls:
    termination: {{ .Values.langgraphFastapi.route.tls.termination }}
    insecureEdgeTerminationPolicy: {{ .Values.langgraphFastapi.route.tls.insecureEdgeTerminationPolicy }}
{{- end }}
