{{- if .Values.finance.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: novacrest-finance-secret
type: Opaque
stringData:
  database.password: {{ .Values.finance.database.password | quote }}
{{- end }}
