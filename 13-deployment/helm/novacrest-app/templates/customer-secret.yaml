{{- if .Values.customer.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: novacrest-customer-secret
type: Opaque
stringData:
  database.password: {{ .Values.customer.database.password | quote }}
{{- end }}
