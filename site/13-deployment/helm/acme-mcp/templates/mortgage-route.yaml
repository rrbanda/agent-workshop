{{- if and .Values.mortgage.enabled .Values.mortgage.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.mortgage.route.name }}
  labels:
    app: {{ .Values.mortgage.name }}
spec:
  to:
    kind: Service
    name: {{ .Values.mortgage.service.name }}
  port:
    targetPort: http
  tls:
    termination: {{ .Values.mortgage.route.tls.termination }}
    insecureEdgeTerminationPolicy: {{ .Values.mortgage.route.tls.insecureEdgeTerminationPolicy }}
{{- end }}
