apiVersion: v1
kind: Service
metadata:
  name: acme-finance-service
  labels:
    app: acme-finance-api
spec:
  type: ClusterIP
  selector:
    app: acme-finance-api
  ports:
  - name: http
    port: 8082
    targetPort: 8082
    protocol: TCP
