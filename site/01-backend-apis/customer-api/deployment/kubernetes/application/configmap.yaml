apiVersion: v1
kind: ConfigMap
metadata:
  name: acme-customer-config
  labels:
    app: acme-customer-api
data:
  database.url: "jdbc:postgresql://postgres-cust:5432/acme_customer"
  database.username: "postgres"
  application.properties: |
    spring.jpa.hibernate.ddl-auto=update
    spring.jpa.show-sql=false
    logging.level.com.customer=INFO
