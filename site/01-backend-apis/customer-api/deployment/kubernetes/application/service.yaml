apiVersion: v1
kind: Service
metadata:
  name: acme-customer-service
  labels:
    app: acme-customer-api
spec:
  type: ClusterIP
  selector:
    app: acme-customer-api
  ports:
  - name: http
    port: 8081
    targetPort: 8081
    protocol: TCP
