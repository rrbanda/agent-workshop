= Module 09: Safety Shields
include::_attributes.adoc[]

== Learning Objectives

* Register safety shields with Llama Stack
* Test content safety directly via the safety API
* Integrate input and output shields into agents

TIP: *Capstone Preview:* In the capstone, you will wrap the mortgage agent with these safety shields to block social-engineering attempts like "How can I forge bank statements?"

== Prerequisites

* xref:03-llama-stack-basics.adoc[Module 03: Llama Stack Basics] completed
* A safety provider available on your Llama Stack server (the default `trustyai_fms` is pre-configured)

== Concepts

*Safety shields* are content classifiers that sit between the user and the LLM. An *input shield* scans the user's message before it reaches the model. An *output shield* scans the model's response before it reaches the user. In Llama Stack, shields are registered once and can be attached to any agent.

This workshop uses the *TrustyAI Guardrails Orchestrator* (`trustyai_fms` provider), which provides regex-based *PII detectors* that catch personally identifiable information such as email addresses, Social Security numbers, and credit card numbers. When PII is detected, the shield returns a violation that blocks the content.

NOTE: If your Llama Stack server has a `llama-guard` provider instead, set `SHIELD_PROVIDER=llama-guard` and `SHIELD_MODEL=<your-model>` in `.env`. Llama Guard uses an LLM to classify content as safe or unsafe across broader harm categories (violence, fraud, etc.) rather than regex PII detection.

== Scripts

[cols="2,4", options="header"]
|===
|Script
|What It Does

|`1_list_models.py`
|List all available models

|`2_list_safety_providers.py`
|List safety providers

|`3_list_shields.py`
|List registered shields

|`4_register_shield.py`
|Register a safety shield

|`5_test_shield.py`
|Test shield with safe and unsafe messages

|`6_agent_shield.py`
|Create an agent with input and output shields
|===

== Step-by-Step

NOTE: *Working directory:* All commands in this module run from `09-safety-shields/`. +
*Services needed:* Llama Stack server with a safety provider (default: `trustyai_fms`).

=== 1. Register a Shield

[source,bash]
----
python 4_register_shield.py
----

=== 2. Test the Shield

[source,bash]
----
python 5_test_shield.py
----

Expected: "What is the weather?" passes; harmful content is flagged as a violation.

=== 3. Agent with Shields

[source,bash]
----
python 6_agent_shield.py
----

The agent blocks unsafe inputs and filters unsafe outputs.

== What You Should See

=== Shield Test (script 5)

[source,text]
----
Testing shield: pii_detector

Test: Clean content (should PASS)
  Input: "What is the weather like today?"
  Result: SAFE - Content passed safety checks

Test: Contains email (should VIOLATE)
  Input: "My email is test@example.com"
  Result: VIOLATION DETECTED

Test: Contains SSN (should VIOLATE)
  Input: "My SSN is 123-45-6789"
  Result: VIOLATION DETECTED

Test: Contains credit card (should VIOLATE)
  Input: "Card number: 4111-1111-1111-1111"
  Result: VIOLATION DETECTED
----

=== Agent with Shield (script 6)

[source,text]
----
User: Give me a sentence that contains the word: aloha
  Output check: PASSED

User: My SSN is 123-45-6789 and my email is user@example.com. Can you help with my loan?
  BLOCKED by input shield: ...
----

== Concepts Applied

* *From Module 03*: Agent creation, `LlamaStackClient`
* *New*: Shield registration, `input_shields`, `output_shields`, safety API

== Troubleshooting

[cols="2,3", options="header"]
|===
|Problem
|Solution

|"Shield not found"
|Verify `SHIELD_ID` matches a registered shield on your Llama Stack server (run `3_list_shields.py` to check)

|All inputs flagged as violations
|Check that `SHIELD_ID` matches the registered shield name

|No safety providers listed
|Your Llama Stack config may need a safety provider -- check the server config
|===

== Key Takeaways

* Shields provide content safety guardrails for LLM agents
* `input_shields` check user messages before they reach the LLM
* `output_shields` check LLM responses before they reach the user
* The `trustyai_fms` provider uses regex-based PII detectors (email, SSN, credit card)
* Alternative: the `llama-guard` provider uses an LLM for broader content safety classification

== Next Module

Proceed to xref:10-evaluations.adoc[Module 10: Evaluations] to measure your agent's quality.
